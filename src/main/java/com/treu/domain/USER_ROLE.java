package com.treu.domain;

// Defines an enumeration for the roles a user can have in the application
public enum USER_ROLE {
    // Represents an administrative user with elevated privileges
    ROLE_ADMIN,

    // Represents a standard user with basic access rights
    ROLE_USER
}
