package com.treu.domain;

// Defines an enumeration for the types of verification methods supported in the application
public enum VerificationType {
    // Represents verification via mobile number (e.g., SMS OTP)
    MOBILE,

    // Represents verification via email address (e.g., email OTP)
    EMAIL
}
