package com.treu.domain;

// Defines an enumeration for the supported payment methods in the application
public enum PaymentMethod {
    // Represents the Razorpay payment gateway (commonly used in India)
    RAZORPAY,

    // Represents the Stripe payment gateway (widely used internationally)
    STRIPE
}