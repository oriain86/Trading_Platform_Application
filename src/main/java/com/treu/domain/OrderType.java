package com.treu.domain;

// Defines an enumeration for the types of orders that can be placed
public enum OrderType {
    // Represents an order to purchase an asset (e.g., buying a coin)
    BUY,

    // Represents an order to sell an asset (e.g., selling a coin)
    SELL
}
